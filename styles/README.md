# 🟡 globals.css - за что отвечает?

globals.css — это минималистичный файл.

Он включает:

@tailwind base;
@tailwind components;
@tailwind utilities;


И если нужно — добавляет:

глобальные reset'ы

html, body стили

кастомные переменные root (если они нужны вообще)

глобальные keyframes

импорты шрифтов (если шрифты кастомные)

И всё 🟦.

Никаких палитр цветов в globals.css — НЕ НУЖНО.
